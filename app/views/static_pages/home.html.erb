<% if logged_in? %>
  <div class="row">
      <section class="user_info">
        <%= render 'shared/user_info' %>
      </section>
      <section class="shift_form">
        <%= render 'shared/shift_form' %>
      </section>
      <div class="col-md-8">
      <h3>Shifts Feed</h3>
      <%= render 'shared/feed' %>
    </div>
  </div>
<% else %>
<h1>Sample App</h1>
  <p>
    This is the home page for David's sample app for 
    <a href="https://www.workforce.com/">www.workforce.com</a>
  </p>
<% end %>

